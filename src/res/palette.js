import colors from './colors';

const palette = {
  heading: {
    color: colors.black,
    fontSize: 20,
    textAlign: 'center',
  },
  text: {
    color: colors.white,
    fontSize: 17,
    textAlign: 'center',
  },
};

export default palette;
